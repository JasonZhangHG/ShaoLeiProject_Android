package com.aidebar.greendaotest.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.qoson.shaoleiproject.GreenDao.ResultDao;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "RESULT_DAO".
*/
public class ResultDaoDao extends AbstractDao<ResultDao, Long> {

    public static final String TABLENAME = "RESULT_DAO";

    /**
     * Properties of entity ResultDao.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property CreatTime = new Property(0, long.class, "CreatTime", true, "_id");
        public final static Property UseTime = new Property(1, int.class, "useTime", false, "DBBeanUserInfoFansName");
    }


    public ResultDaoDao(DaoConfig config) {
        super(config);
    }
    
    public ResultDaoDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"RESULT_DAO\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: CreatTime
                "\"DBBeanUserInfoFansName\" INTEGER NOT NULL );"); // 1: useTime
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"RESULT_DAO\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ResultDao entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCreatTime());
        stmt.bindLong(2, entity.getUseTime());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ResultDao entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCreatTime());
        stmt.bindLong(2, entity.getUseTime());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public ResultDao readEntity(Cursor cursor, int offset) {
        ResultDao entity = new ResultDao( //
            cursor.getLong(offset + 0), // CreatTime
            cursor.getInt(offset + 1) // useTime
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ResultDao entity, int offset) {
        entity.setCreatTime(cursor.getLong(offset + 0));
        entity.setUseTime(cursor.getInt(offset + 1));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ResultDao entity, long rowId) {
        entity.setCreatTime(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ResultDao entity) {
        if(entity != null) {
            return entity.getCreatTime();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ResultDao entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
